# MakeIT Inventory API

REST API för lagerhantering (MakeIT). Byggt med NestJS + Prisma.

## Tech
- NestJS
- Prisma
- PostgreSQL (alt. SQLite lokalt)
- JWT Auth

## Start
1. npm install
2. Skapa .env (se .env.example)
3. npx prisma migrate dev
4. npm run start:dev
